### Tools necessary to run the program:

Python:
3.11.9

CMake:
https://cmake.org/download/
pip install cmake

dlib:
pip install dlib

face_recognition:
pip install face_recognition

OpenCV:
pip install opencv